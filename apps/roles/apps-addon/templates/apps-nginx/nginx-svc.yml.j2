apiVersion: v1
kind: Service
metadata:
  name: {{ nginx_svcName }}
  namespace: {{ nginx_nameSpace }}
  labels:
    app: {{ nginx_label }}
spec:
  type: NodePort
  selector:
    app: {{ nginx_label }}
  ports:
  - name: http
    port: 80
    protocol: TCP
